<Window x:Class="SNOMEDLookup.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SNOMED Lookup Settings"
        Width="420" Height="220"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="16" FontWeight="SemiBold" Text="Hotkey" />

        <StackPanel Grid.Row="1" Margin="0,10,0,0">
            <TextBlock Text="Initial version uses Ctrl+Shift+L. Hotkey picker UI will be added in a follow-up." Foreground="Gray"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="90" Margin="0,0,8,0" Click="Cancel_Click">Cancel</Button>
            <Button Width="90" Click="Save_Click">Save</Button>
        </StackPanel>
    </Grid>
</Window>
